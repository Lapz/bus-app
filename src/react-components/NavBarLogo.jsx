import React from 'react';

const NavBarLogo = ({logoText}) => (
    <div className="nav-left">
        <a className="nav-item">
            {logoText}
        </a>
    </div>
)

export default NavBarLogo